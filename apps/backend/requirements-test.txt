# Dépendances pour lancer les tests en local (sans Docker), avec SQLite.
# Volontairement SANS psycopg2-binary : les tests utilisent SQLite (voir
# tests/conftest.py), et psycopg2 n'a pas toujours de wheel précompilé
# disponible pour toutes les versions/plateformes Python (nécessite pg_config
# sinon). En production, le conteneur Docker (Linux) l'installe sans problème
# via requirements.txt.

fastapi==0.115.0
uvicorn[standard]==0.32.0
sqlalchemy==2.0.35
celery==5.6.3
pydantic-settings==2.5.2
python-dotenv==1.0.1
docker==7.1.0
kubernetes==36.0.2
websockets==13.1
redis==5.2.1

pytest==8.3.3
pytest-mock==3.14.0
pytest-asyncio==0.24.0
httpx==0.27.2
